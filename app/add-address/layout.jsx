export const metadata = {
  title: "Add Address - Shazi Jewels",
  description: "Add a new shipping address to your Shazi Jewels account for faster checkout.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AddAddressLayout({ children }) {
  return <>{children}</>;
}
